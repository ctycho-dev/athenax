
export enum UserRole {
    ADMIN = 'admin',
    BD = 'bd',
    USER = 'user',
}

export enum ReportState {
    SUBMITTED = 'Submitted',
    CHECKING = 'Checking',
    WRITING = 'Writing',
    UPDATE_INFO = 'Update Info',
    COMPLETED = 'Completed',
    REJECTED = 'Rejected'
}

export enum ArticleState {
    DRAFT = 'draft',
    PUBLISHED = 'published',
    ARCHIVED = 'archived',
}